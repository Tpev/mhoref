cd /var/www/mhoref
sudo -u www-data git pull origin main
sudo -u www-data bash -lc "cd /var/www/mhoref && composer install --no-dev --optimize-autoloader"
sudo -u www-data bash -lc "cd /var/www/mhoref && php artisan migrate --force"
sudo -u www-data bash -lc "cd /var/www/mhoref && npm ci && npm run build"
sudo -u www-data bash -lc "cd /var/www/mhoref && php artisan optimize && php artisan queue:restart"
sudo systemctl reload nginx
